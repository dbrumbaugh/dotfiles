#!/usr/local/bin/ksh

browser=${BROWSER-surf}

[[ -z "${1-""}" ]] && exit 1

case "$1" in
    *youtube.com/watch*|*youtu.be*|*odysee.com*|*utreon.com/embed/*)
        mpv -quiet $1 ;;
    *) "$BROWSER" "$1"
esac

