#!/usr/local/bin/ksh93

browser="qutebrowser"

[[ -z "${1-""}" ]] && exit 1

case "$1" in
    *youtube.com/watch*|*youtu.be*|*odysee.com*|*utreon.com/embed/*|*youtube-nocookie.com/embed/*|*youtube.com/embed/*)
        mpv -quiet $1 ;;
    *) "$browser" "$1"
esac

